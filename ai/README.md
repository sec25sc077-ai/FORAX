# FORAX AI Assistant

The AI layer is a suggestion interface, not the execution authority.

Flow:
Natural language -> AI suggests FORAX -> compiler validates -> approved AST -> runtime.

The AI must not invent executable keywords or directly execute generated TypeScript.
